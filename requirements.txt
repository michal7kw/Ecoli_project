# Core
numpy>=1.24
scipy>=1.10          # also the FBA solver: linprog(method="highs") in moma/fluxome.py
scikit-learn>=1.3    # LASSO, for the proteome, metabolome and phenome modules
pandas>=2.0
pyarrow>=12.0        # Parquet mirrors of the wide matrices

# The paper's Supplementary Data files ship as .xlsx and are read directly, by three
# scripts. `pandas.read_excel` dispatches to openpyxl in 08_methods_faithful_eval.py,
# which needs Supplementary Data 1's `Growth Phase` column to build the paper's
# exponential-phase subset; networks_paper.load_paper_networks reads all of Data 2's
# graphs for the proteome layer; fluxome.load_paper_medium_bounds reads Data 4.
# Required by the MAIN chain, not optional -- scripts/04_reproduce.py aborts in
# eval_fluxome without it.
openpyxl>=3.1

# Transcriptome module (relaxation RNN) -- the only torch user here.
# A CPU build runs everything; only `scripts/03_train_moma.py --device cuda` needs CUDA.
# On Linux the default wheel below already bundles CUDA. On WINDOWS it does not, and
# `torch.cuda.is_available()` is False even with a working driver -- install instead from
#   pip install torch --index-url https://download.pytorch.org/whl/cu130
# and confirm the build tag reads `2.13.0+cu130`, not `2.13.0`.
torch>=2.0

# Optional, and genuinely optional: the figure atlas (ecomics/plots.py +
# ecomics/figures/, driven by scripts/15_figures.py). Nothing in the
# 00 -> 01 -> (02|03|04) chain imports it, and ecomics/plots.py raises a message
# naming this install line if it is missing.
matplotlib>=3.7

# Deliberately NOT required:
#   requests    -- urllib handles the GETs in acquire/ perfectly well
#   cobra       -- FBA is solved directly with scipy.optimize.linprog (HiGHS);
#                  moma/fluxome.py builds the LP from BiGG iJO1366 itself
#   statsmodels -- loess is implemented in pipeline/platform.py
#
# R/Bioconductor (affy, preprocessCore, impute, mclust, ecoliasv2cdf) is used
# ONLY by tools/*.R for independent validation and the one-time CDF export.
# Nothing at runtime depends on it.
